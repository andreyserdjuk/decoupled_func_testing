sudo: false
language: php

php:
  - 5.6
  - 7.1

services:
  - mysql

env:
  - DB=mysql
  - DB_USER
  - DB_HOST
  - DB_PORT
  - DB_NAME
  - DB_USER
  - DB_PASSWORD

before_script:
  - composer install

script:
  - DB_USER=travis DB_PORT=3306 DB_HOST=127.0.0.1